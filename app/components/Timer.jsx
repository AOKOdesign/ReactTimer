var React = require('react');
var Clock = require('Clock');

var Timer = React.createClass({
  render: function () {
    return (
      <div>
        <h3>Timer.jsx</h3>
      </div>
    );
  }
});

module.exports = Timer;
